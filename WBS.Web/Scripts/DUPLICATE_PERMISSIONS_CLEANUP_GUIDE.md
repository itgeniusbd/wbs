# Duplicate Permissions Cleanup Guide

## ?????? (Problem)
Permissions table ? duplicate entries ???? ????? ??? ??? ???:
- SQL script ????????? run ??? ???
- Migration ????????? apply ??? ???

## ?????? (Solution) - ????? ??????

### ?? ?????? ?: Simple Cleanup Script (??????? ??????) ? RECOMMENDED

#### ??? ?: Check ????
```sql
-- File: WBS.Web\Scripts\CheckDuplicatePermissions.sql
```
SSMS ?? ?? file open ??? **Execute** ?????

#### ??? ?: Simple Cleanup Run ????
```sql
-- File: WBS.Web\Scripts\SimpleCleanupDuplicatePermissions.sql
```

?? script:
- ? **Automatic backup** ???? ???? (`Permissions_Backup_BeforeCleanup`)
- ? **Step-by-step** progress ??????
- ? **Safe transaction** - error ??? automatic rollback
- ? **Detailed logging** ??????? step ??
- ? **Verification** ???? ????

---

### ?? ?????? ?: Manual Review & Cleanup (Manual control ?????)

#### ??? ?: Review ???? ?? delete ???
```sql
-- File: WBS.Web\Scripts\ManualCleanupPermissions.sql
```
???????? run ???? ???? **preview** ?????? - ???? delete ??? ???

#### ??? ?: Review ???? ?? Uncomment ????
File ?? ????? comment ???? DELETE section uncomment ????:
```sql
/*
-- ============================================
-- UNCOMMENT THIS SECTION TO EXECUTE CLEANUP
-- ============================================
*/
```
?? comment markers remove ???? ??? ???? run ?????

---

### ? ?????? ?: Original Cleanup Script (Advanced)

```sql
-- File: WBS.Web\Scripts\CleanupDuplicatePermissions.sql
```
Updated script - RolePermissions table ?? ???? fix ??? ???????

---

## ?? Troubleshooting

### Error: "Invalid column name 'Id'" in RolePermissions
? **Fixed!** SimpleCleanupDuplicatePermissions.sql ?? ?? ?????? fix ??? ???????

RolePermissions table ? `Id` column ??? - ??? composite key table (`RoleId`, `PermissionId`)?

### Script Run ????? ??
1. ? Database select ??? ??? ???? check ???? (top left dropdown)
2. ? Script ?? ?? text select ??? run ????
3. ? SSMS ?? **Execute** button click ???? (F5)

### "Permission denied" error
Administrator ?????? SSMS run ?????

### Still have duplicates after cleanup
1. Check ???? script ??? ??????? run ?????? ????
2. Messages tab ? error ?????
3. Manual cleanup script try ????

---

## ?? Pre-Cleanup Checklist

- [ ] ? Database backup ?????? ??????
- [ ] ? CheckDuplicatePermissions.sql ????? duplicates confirm ??? ??????
- [ ] ? Development/Staging environment ? test ??? ?????? (production ???)
- [ ] ? ??? user currently logged in ???

---

## ?? Post-Cleanup Verification

### ?. Check for remaining duplicates
```sql
SELECT Module, Action, COUNT(*) as Count
FROM Permissions
GROUP BY Module, Action
HAVING COUNT(*) > 1;
```
Result: **(0 rows)** ???? ????

### ?. Count total permissions
```sql
SELECT COUNT(*) as TotalPermissions FROM Permissions;
```

### ?. Check RolePermissions
```sql
SELECT 
    r.Name as RoleName,
    COUNT(DISTINCT rp.PermissionId) as UniquePermissions
FROM AspNetRoles r
LEFT JOIN RolePermissions rp ON r.Id = rp.RoleId
GROUP BY r.Name
ORDER BY r.Name;
```

### ?. Test Application
1. Application restart ????
2. Different roles ????? login ????
3. Access control test ????

---

## ??? Cleanup After Success

??? ?????? ??? ????:

```sql
-- Backup table delete ???? (optional)
DROP TABLE Permissions_Backup_BeforeCleanup;
```

---

## ?? ??? ?????? ???

### Script failed midway
- ? Transaction automatically rollback ???
- ? ???? data change ?????
- ? Error message ????? Messages tab ?

### Data corrupted (unlikely)
Backup ???? restore ????:
```sql
-- If you have Permissions_Backup_BeforeCleanup
DELETE FROM Permissions;
INSERT INTO Permissions 
SELECT * FROM Permissions_Backup_BeforeCleanup;
```

???? full database backup ???? restore ?????

---

## ?? Quick Start (Recommended Path)

1. Open SSMS
2. Select your database
3. Open: `SimpleCleanupDuplicatePermissions.sql`
4. Click **Execute** (F5)
5. Watch the progress messages
6. Verify success message

Done! ?

---

## ?? Expected Results

Before cleanup:
- Total permissions: ~200-300 (with duplicates)
- Duplicate modules: 10-15

After cleanup:
- Total permissions: ~87 (unique)
- Duplicate modules: 0
- All RolePermissions updated correctly

---

## ???????? ??????? ????

1. ? Migration scripts ???? **?????** run ????
2. ? Check migration history:
   ```bash
   dotnet ef migrations list
   ```
3. ? SQL scripts manually run ???? ??? check ???? duplicate ??? ????
4. ? Production ? run ???? ??? staging/dev ? test ????
