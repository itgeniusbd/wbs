@model IEnumerable<WBS.Web.Models.Slider>

@{
    ViewData["Title"] = "Sliders";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h2><i class="fas fa-images me-2"></i>Homepage Sliders</h2>
    <a asp-action="Create" class="btn btn-primary">
        <i class="fas fa-plus me-2"></i>Create New Slider
    </a>
</div>

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show shadow-sm" role="alert" id="successAlert" style="border-left: 4px solid #198754;">
        <div class="d-flex align-items-center">
            <i class="fas fa-check-circle me-3" style="font-size: 1.5rem;"></i>
            <div class="flex-grow-1">
                <strong>Success!</strong>
                <p class="mb-0">@TempData["Success"]</p>
            </div>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show shadow-sm" role="alert" style="border-left: 4px solid #dc3545;">
        <div class="d-flex align-items-center">
            <i class="fas fa-exclamation-circle me-3" style="font-size: 1.5rem;"></i>
            <div class="flex-grow-1">
                <strong>Error!</strong>
                <p class="mb-0">@TempData["Error"]</p>
            </div>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="card shadow">
    <div class="card-body">
        <div class="table-responsive">
            <table class="table table-hover">
                <thead class="table-light">
                    <tr>
                        <th style="width: 100px;">Image</th>
                        <th>Title</th>
                        <th>Subtitle</th>
                        <th>Buttons</th>
                        <th>Order</th>
                        <th>Status</th>
                        <th style="width: 150px;">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null && Model.Any())
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @if (!string.IsNullOrEmpty(item.ImageUrl))
                                    {
                                        <img src="@item.ImageUrl" alt="@item.Title" class="img-thumbnail" style="max-height: 60px;" onerror="this.src='/images/no-image.png'" />
                                    }
                                </td>
                                <td>
                                    <strong>@item.Title</strong>
                                    @if (!string.IsNullOrEmpty(item.TitleBn))
                                    {
                                        <br /><small class="text-muted bangla-text" style="font-family: 'Noto Sans Bengali', sans-serif;">@item.TitleBn</small>
                                    }
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(item.Subtitle))
                                    {
                                        <span>@item.Subtitle</span>
                                        @if (!string.IsNullOrEmpty(item.SubtitleBn))
                                        {
                                            <br /><small class="text-muted bangla-text" style="font-family: 'Noto Sans Bengali', sans-serif;">@item.SubtitleBn</small>
                                        }
                                    }
                                </td>
                                <td>
                                    @if (!string.IsNullOrEmpty(item.ButtonText))
                                    {
                                        <span class="badge bg-primary">@item.ButtonText</span>
                                    }
                                    @if (!string.IsNullOrEmpty(item.SecondButtonText))
                                    {
                                        <span class="badge bg-secondary">@item.SecondButtonText</span>
                                    }
                                </td>
                                <td>
                                    <span class="badge bg-info">@item.DisplayOrder</span>
                                </td>
                                <td>
                                    @if (item.IsActive)
                                    {
                                        <span class="badge bg-success"><i class="fas fa-check me-1"></i>Active</span>
                                    }
                                    else
                                    {
                                        <span class="badge bg-secondary"><i class="fas fa-times me-1"></i>Inactive</span>
                                    }
                                </td>
                                <td>
                                    <div class="btn-group" role="group">
                                        <a asp-action="Edit" asp-route-id="@item.Id" class="btn btn-sm btn-outline-primary" title="Edit">
                                            <i class="fas fa-edit"></i>
                                        </a>
                                        <button type="button" class="btn btn-sm btn-outline-danger" onclick="deleteSlider(@item.Id)" title="Delete">
                                            <i class="fas fa-trash"></i>
                                        </button>
                                    </div>
                                </td>
                            </tr>
                        }
                    }
                    else
                    {
                        <tr>
                            <td colspan="7" class="text-center py-5">
                                <i class="fas fa-images fa-3x text-muted mb-3 d-block"></i>
                                <h5 class="text-muted">No sliders found</h5>
                                <p class="text-muted mb-3">Create your first slider to get started!</p>
                                <a asp-action="Create" class="btn btn-primary">
                                    <i class="fas fa-plus me-2"></i>Create New Slider
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="alert alert-info mt-3">
    <i class="fas fa-info-circle me-2"></i>
    <strong>Tip:</strong> Recommended image size for sliders is <strong>1920x1080px</strong> or similar aspect ratio.
</div>

@section Scripts {
    <script>
        // Auto-hide success message after 5 seconds
        const successAlert = document.getElementById('successAlert');
        if (successAlert) {
            setTimeout(function() {
                const bsAlert = new bootstrap.Alert(successAlert);
                bsAlert.close();
            }, 5000);
        }

        function deleteSlider(id) {
            if (confirm('Are you sure you want to delete this slider? This action cannot be undone.')) {
                var form = document.createElement('form');
                form.method = 'POST';
                form.action = '/Admin/Sliders/Delete/' + id;

                var token = document.querySelector('input[name="__RequestVerificationToken"]');
                if (token) {
                    var input = document.createElement('input');
                    input.type = 'hidden';
                    input.name = '__RequestVerificationToken';
                    input.value = token.value;
                    form.appendChild(input);
                }

                document.body.appendChild(form);
                form.submit();
            }
        }
    </script>
}

<form method="post" style="display:none;">
    @Html.AntiForgeryToken()
</form>
